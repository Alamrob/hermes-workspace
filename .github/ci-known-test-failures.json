{
  "description": "Known pre-existing Vitest failures. Required CI permits only this exact set while blocking every new failure.",
  "minimumTestFiles": 110,
  "minimumTests": 700,
  "knownFailureFiles": [
    "e2e/chat-flicker-duplicate.spec.ts",
    "e2e/chat-thinking-state.spec.ts",
    "e2e/conductor-mobile-rendering.spec.ts"
  ],
  "knownFailureNames": [
    "ChatComposer context controls surfaces workspace and reasoning controls next to the model picker",
    "GET /api/mcp/hub-search — query parsing clamps limit to 100",
    "GET /api/mcp/hub-search — query parsing defaults invalid source to all",
    "GET /api/mcp/hub-search — query parsing passes q, source, limit to unifiedSearch",
    "GET /api/mcp/hub-search — query parsing uses defaults when params absent",
    "GET /api/mcp/presets returns 200 with source=invalid + error fields when user file is malformed",
    "HERMES_HOME override for path resolution uses HERMES_HOME to resolve cache file path",
    "buildDisplayEntries does not attach trailing persisted tool-only assistant messages to the last text reply",
    "external-memory-browser approves, rejects, and deletes candidates from the review queue",
    "external-memory-browser edits candidate text and refreshes its content hash",
    "external-memory-browser lists provider candidates from the review queue sqlite database",
    "external-memory-browser returns per-state totals with candidate lists for filter badges",
    "external-memory-browser searches candidate text and metadata for a registered provider",
    "getStateDir falls back to CLAUDE_HOME/workspace when only CLAUDE_HOME is set",
    "getStateDir prefers HERMES_HOME over CLAUDE_HOME",
    "getStateDir prefers HERMES_WORKSPACE_STATE_DIR over everything",
    "getStateDir returns HERMES_WORKSPACE_STATE_DIR when set",
    "getStateDir trims whitespace from env values",
    "getStateDir uses HERMES_HOME/workspace when HERMES_WORKSPACE_STATE_DIR is not set",
    "getTrailingToolOnlyTurnSummary detects hidden trailing tool-only messages after the final assistant response",
    "getTrailingToolOnlyTurnSummary returns null when the thread already ends with assistant text",
    "i18n translations exposes readable locale labels for contributor-targeted languages",
    "kanban-backend auto-detect prefers Hermes backend when Hermes CLI and canonical storage are present",
    "kanban-backend auto-detect uses Hermes storage directly when the CLI is unavailable",
    "kanban-backend creates and updates Hermes tasks through canonical kanban.db path",
    "kanban-backend creates native Kanban tasks with parent links and idempotency without using Workspace board files",
    "kanban-backend projects native Kanban tasks without collapsing statuses or dependency/run metadata",
    "kanban-backend resolves canonical Kanban paths from legacy profile-home env fallback too",
    "legacy /api/claude-config alias GET aliases provider.maskedCredentials to provider.maskedKeys for the legacy /settings page",
    "local-provider-discovery isProviderConfigured uses YAML.parse and reads from CLAUDE_HOME",
    "models route reads default model from CLAUDE_HOME config using YAML.parse",
    "models route reads nested model object syntax from config using YAML.parse",
    "normalizeSwarmRuntime resolves semantic wrapper aliases from the roster",
    "openaiChat sends Hermes session continuity headers even without a bearer token",
    "readHubSources invalidates cache after file changes",
    "readHubSources reads valid user file and merges with built-ins",
    "readHubSources rejects duplicate ids",
    "readHubSources rejects reserved built-in ids",
    "readHubSources returns source=invalid for bad id format",
    "readHubSources returns source=invalid for http:// url",
    "readHubSources returns source=invalid for malformed JSON, preserves file",
    "readHubSources returns source=invalid for wrong version",
    "readPresets cache invalidates when file mtime+size changes",
    "readPresets cache invalidates when size changes even if mtime is identical",
    "readPresets defaults category to Custom when missing",
    "readPresets honors HERMES_HOME override for the user file path",
    "readPresets reads valid JSON from the user file",
    "readPresets rejects bad homepage URL",
    "readPresets rejects bad id format",
    "readPresets rejects duplicate ids with a path-prefixed error",
    "readPresets rejects http transport carrying a stdio command",
    "readPresets rejects preset with unknown category",
    "readPresets returns source=invalid for malformed user JSON and preserves the file unchanged",
    "readPresets returns source=invalid when user file exists but is permission-denied (EACCES)",
    "readPresets returns source=invalid when user file path is a dangling symlink",
    "readPresets surfaces unknown top-level fields as warnings (not errors)",
    "swarm-memory module scaffolds worker memory files in canonical Hermes profile path",
    "swarm-memory module writes worker handoff and skips shared mirror when requested"
  ]
}
